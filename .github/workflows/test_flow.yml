name: check_flow
on: push

jobs:
  testing:
    uses: surfstudio/flutter-ci-workflows/.github/workflows/testing.yml@main
    secrets:
      CODECOV_TOKEN: ${{secrets.CODECOV_TOKEN}}

  analysis:
    uses: surfstudio/flutter-ci-workflows/.github/workflows/analysis.yml
